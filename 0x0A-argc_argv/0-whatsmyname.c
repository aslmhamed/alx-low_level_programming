#include <stdio.h>

/**
 * main - a program that prints it`s name
 * @argc: int.
 * @argv: list
 * Return: Always 0.
 */

int main(int argc, char const **argv)
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
